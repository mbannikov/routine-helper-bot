spring:
  datasource:
    driverClassName: org.h2.Driver
    url: jdbc:h2:file:/tmp/test-db;DB_CLOSE_ON_EXIT=FALSE;IFEXISTS=FALSE;
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate.ddl-auto: update